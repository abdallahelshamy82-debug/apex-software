# 🚀 دليل تسليم المشروع وإعداده للنشر على متجر Google Play
### مشروع: Apex Software Client Portal & Generative AI Copilot
**تاريخ الإعداد: سبتمبر 2026**

---

## 📌 1. نظرة عامة على محتويات المشروع (Project Structure)

المشروع مبني بأحدث المعايير البرمجية ويتكون من:

```text
Apex_Software_Project/
├── apex-app/                  # تطبيق الهاتف والويب المبني بـ React Native (Expo SDK 52)
│   ├── src/
│   │   ├── app/               # شاشات التطبيق عبر Expo Router
│   │   │   ├── index.tsx          # شاشة البداية والترحيب
│   │   │   ├── login.tsx          # تسجيل الدخول الموحد (بصمة + بريد + جوجل)
│   │   │   ├── dashboard.tsx      # لوحة تحكم العميل وخارطة طريق المشروع
│   │   │   ├── copilot.tsx        # مستشار الذكاء الاصطناعي التوليدي (AI Copilot 2.0)
│   │   │   ├── portfolio.tsx      # ⭐ صفحة الهوية وسابقة الأعمال المخصصة
│   │   │   ├── privacy.tsx        # 🛡️ صفحة سياسة الخصوصية وشروط الاستخدام
│   │   │   ├── estimator.tsx      # حاسبة تسعير المشاريع التفاعلية
│   │   │   ├── invoices.tsx       # إدارة الفواتير والمدفوعات والمرفقات
│   │   │   ├── chat.tsx           # المحادثة الحية مع فريق الدعم الفني
│   │   │   ├── admin.tsx          # لوحة التحكم الإدارية الكاملة للمديرين
│   │   │   └── settings.tsx       # إعدادات المظهر، اللغات، والصلاحيات
│   │   ├── services/api.ts        # خدمات الاتصال بالباك إند و AI
│   │   └── utils/                 # محركات البصمة، الاهتزازات (Haptics)، والإشعارات
│   ├── app.json               # إعدادات الحزمة (com.apexsoftware.portal) والأيقونات والصلاحيات
│   └── eas.json               # إعدادات البناء السحابي لـ APK و AAB
│
├── apex-backend/              # خادم الـ API والذكاء الاصطناعي (Node.js & Express & SQLite)
│   ├── server.js              # الخادم الرئيسي والمصادقة والمسارات ومحادثات Socket.io
│   ├── aiCopilot.js           # محرك الذكاء الاصطناعي (Gemini 1.5 Flash + المحرك الدلالي)
│   ├── emailService.js        # قوالب البريد الإلكتروني الفاخرة
│   └── database.sqlite        # قاعدة البيانات المهيكلة والمجهزة
│
├── GOOGLE_PLAY_AND_HANDOVER_GUIDE.md # هذا الدليل الشامل
└── run_dev.bat / run_dev.sh   # سكربتات التشغيل الفوري بنقرة واحدة
```

---

## 🎨 2. كيفية تخصيص "صفحة الهوية وسابقة الأعمال" (Portfolio & Identity)

الملف المخصص للهوية وسابقة الأعمال هو:
`apex-app/src/app/portfolio.tsx`

الشاشة جاهزة بالكامل ومربوطة بأزرار لوحة التحكم والإعدادات، وتتضمن العناصر التالية القابلة للتعديل بسهولة:

### أ. سابقة الأعمال (Portfolio Projects):
يمكن للمطور أو المصمم تعديل مصفوفة `PORTFOLIO_PROJECTS` في أول الملف بإضافة المشاريع الحقيقية:
```typescript
{
  id: 'p1',
  category: 'mobile', // 'mobile' | 'web' | 'ai'
  title: 'اسم المشروع الحقيقي',
  client: 'اسم العميل أو الجهة',
  desc: 'وصف مختصر ومبهر للمشروع والتحدي الذي تم حله',
  tech: ['React Native', 'Node.js', 'Paymob', 'AWS'],
  image: 'https://... أو مسار صورة محلية من assets',
  metrics: 'أرقام النجاح (مثال: +100k تحميل • تقييم 4.9)',
  featured: true,
}
```

### ب. خدمات وخبرات الشركة (Core Services):
تعديل مصفوفة `SERVICES` لتحديد تخصصات Apex Software (تطوير تطبيقات، حلول ويب، ذكاء اصطناعي، أمن سحابي).

### ج. آراء العملاء والشركاء (Testimonials):
تعديل مصفوفة `TESTIMONIALS` لإبراز التقييمات الواقعية مع أسماء ومناصب العملاء.

---

## 📱 3. متطلبات واشتراطات متجر Google Play الإلزامية (Play Store Checklist)

قبل رفع التطبيق على Google Play Console، يجب استيفاء البنود التالية:

### 1. رابط سياسة الخصوصية (Privacy Policy URL) - إلزامي 🚨
- متجر جوجل بلاي يرفض فوراً أي تطبيق يستخدم صلاحيات (البصمة، الميكروفون، الإشعارات) بدون رابط سياسة خصوصية فعال.
- **تم تجهيز ذلك بالكامل:**
  - داخل التطبيق: شاشة مخصصة على الرابط الداخلي `/privacy`.
  - على الويب: مسار مخصص في الباك إند: `http://your-server-domain/privacy`.
  - بمجرد رفع الباك إند على سيرفر حقيقي، ضع الرابط (مثال: `https://api.apexsoftware.com/privacy`) داخل حقل **Privacy Policy** في Google Play Console.

### 2. الأصول الرسومية المطلوبة للمتجر (Graphic Assets):
| العنصر | الأبعاد | الصيغة | متطلبات جوجل |
|---|---|---|---|
| **أيقونة التطبيق (App Icon)** | 512 × 512 px | PNG 32-bit (حتى 1MB) | خلفية ممتلئة بدون شفافية، لا تضع زوايا دائرية (جوجل يضيفها تلقائياً). |
| **البانر الترويجي (Feature Graphic)** | 1024 × 500 px | PNG أو JPEG (حتى 15MB) | بانر جذاب يعبر عن هوية Apex، بدون نصوص صغيرة جداً. |
| **لقطات الشاشة (Phone Screenshots)** | 1080 × 1920 px أو 1080 × 2400 px | PNG / JPEG (نسبة 9:16 أو 16:9) | لقطات شاشات التطبيق: المستشار الذكي، لوحة تحكم المشاريع، الفواتير، وصفحة الهوية (على الأقل لقطتين، ويُفضل 4 إلى 8 لقطات). |

### 3. إعدادات الحزمة في `app.json`:
- اسم الحزمة (Package Name): `com.apexsoftware.portal` (جاهز في `app.json`).
- إصدار التطبيق (Version): `1.0.0`، و `versionCode: 1`. (عند رفع أي تحديث مستقبلي، يجب زيادة `versionCode` إلى 2، 3.. وهكذا).

### 4. استخراج حزمة الإنتاج المعتمدة (Android App Bundle - AAB):
جوجل بلاي يشترط ملف **AAB**، وتم تهيئة ملف `eas.json` لهذا الغرض:
```bash
cd apex-app
eas build -p android --profile production
```

### 5. تسجيل الدخول بجوجل في بيئة الإنتاج المستقلة (Standalone Google Sign-In):
1. انسخ بصمة `SHA-1` التي تظهر في لوحة تحكم Expo/EAS بعد انتهاء البناء.
2. ادخل على **Google Cloud Console** -> **APIs & Services** -> **Credentials**.
3. أضف `OAuth 2.0 Client ID` جديد لنوع **Android**، وضع اسم الحزمة `com.apexsoftware.portal` وبصمة `SHA-1`.

### 6. نقل خادم الـ API إلى سيرفر سحابي (Production API Deployment):
حالياً يتصل التطبيق بـ `http://192.168.1.141:3000` أو `localhost:3000`.
قبل نشر التطبيق للمستخدمين في العالم:
1. ارفع مجلد `apex-backend` على خادم سحابي VPS (مثل Hetzner أو DigitalOcean) أو منصات استضافة مثل (Render / Railway / AWS).
2. افتح ملف `apex-app/src/services/api.ts` وغيّر السطر:
   ```typescript
   export const BASE_URL = 'https://api.yourdomain.com';
   ```

---

## 🛠️ 4. التشغيل المحلي للمشروع والتطوير

### لتشغيل الباك إند:
```bash
cd apex-backend
npm install
node server.js
```

### لتشغيل تطبيق الموبايل والويب:
```bash
cd apex-app
npm install
npx expo start
```

---

## 🛡️ 5. منظومة الحماية والأمان المطبقة في المشروع (Security Hardening & Defense-in-Depth)

المشروع محمي بـ **8 طبقات أمنية صارمة** لحماية البيانات، منع الاختراق، ومنع الهندسة العكسية:

1. **جدار الحماية وعزل الترويسات (Helmet & Anti-Fingerprinting):**
   - تفعيل ترويسات الأمان لمنع هجمات الاختطاف (`X-Frame-Options: SAMEORIGIN`) ومنع التخمين (`X-Content-Type-Options: nosniff`).
   - إخفاء هوية الخادم (`X-Powered-By: Express`) لمنع المهاجمين من استكشاف تقنيات السيرفر.

2. **الحماية من هجمات حجب الخدمة والتخمين (Rate Limiting & Anti-Brute-Force):**
   - حماية تسجيل الدخول واستعادة كلمة المرور بحد أقصى 20 محاولة لكل 15 دقيقة لمنع تخمين كلمات المرور (Brute-force).
   - حماية مستشار الذكاء الاصطناعي من الاستنزاف بحد أقصى 30 طلباً لكل 15 دقيقة.
   - جدار حماية عام لكافة الـ APIs لحماية السيرفر من هجمات الـ DDoS.

3. **حماية رفع الملفات الصارمة (Secure Uploads & Strict Whitelist):**
   - قائمة بيضاء محددة بدقة: صور (`.jpg, .png, .webp, .gif`)، مستندات (`.pdf, .doc, .docx`)، وأصوات (`.mp3, .m4a, .wav`).
   - حظر كامل للملفات التنفيذية والبرمجية لمنع هجمات الـ RCE و Stored XSS (`.exe, .bat, .sh, .php, .html, .svg, .js`).
   - إعادة تسمية عشوائية مشفرة (`Date_CryptoHex.ext`) لمنع هجمات التلاعب بالمسارات (Directory Traversal).
   - تحديد سقف حجم الملف بـ 25 ميجابايت لمنع استنزاف مساحة السيرفر.

4. **منع الوصول غير المصرح به للبيانات (IDOR & Access Control):**
   - حماية مسار المحادثات `/api/messages/:userId` فلا يستطيع أي مستخدم قراءة رسائل غيره.
   - حماية فواتير الطباعة `/invoice-print/:id` بطلب توكن مشفر والتحقق من ملكية الفاتورة.
   - حظر الوصول لأي ملفات حساسة مثل `.env` أو `database.sqlite`.

5. **التشفير والمفاتيح الرقمية (Cryptographic Token Security):**
   - تشفير كلمات المرور بخوارزمية `bcryptjs` مع 10 دورات تشفير (Salt).
   - مفتاح سري عالي الإنتروبيا `JWT_SECRET` مع عمر افتراضي للتوكنات `expiresIn: '30d'`.

6. **منع حقن قواعد البيانات (SQL Injection Immunity):**
   - جميع استعلامات قاعدة البيانات SQLite مجهزة بنظام الاستعلامات المعلمية المجهزة (Parameterized Queries باستخدام `?`) لمنع حقن كود الـ SQL.

7. **حماية كود الموبايل من الهندسة العكسية (Hermes Bytecode & Obfuscation):**
   - في بيئة إنتاج React Native، يقوم محرك **Hermes Engine** بتحويل كود JavaScript بالكامل إلى **Bytecode ثنائي مسبق التجميع (.hbc)** قبل بناء ملف الأندرويد.
   - عند فك ضغط تطبيق الأندرويد من قِبل أي شخص، لن يجد ملفات كود المصدر `.ts` أو `.js`، بل كود مجمع غير قابل للقراءة، مع تشويش وتصغير أسماء الدوال عبر ProGuard/R8.
