# 🚀 دليل المطور الشامل لتشغيل ورفع منصة Apex Software
### (Developer Handoff, Portfolio Setup & Google Play Store Release Guide)

مرحباً بك! هذا الدليل موجه ومُعد خصيصاً للمطور / الصديق للبدء فوراً في تشغيل المشروع، إضافة معرض الأعمال، وتجهيز التطبيق للرفع على متجر **Google Play Store**.

---

## 📁 1. هيكلية المشروع (Project Architecture)

المشروع مقسم إلى جزأين رئيسيين:

```
New folder/
├── apex-app/                  # تطبيق الهاتف والويب (Frontend)
│   ├── src/
│   │   ├── app/               # صفحات ومسارات التطبيق (Expo Router)
│   │   │   ├── portfolio.tsx  # 🎯 شاشة معرض الأعمال والهوية (المكان المطلوب لتعديل المعرض)
│   │   │   ├── admin.tsx      # لوحة تحكم الأدمن والتحليلات وإدارة الطلبات
│   │   │   ├── dashboard.tsx  # لوحة تحكم العميل والمخرجات الحية
│   │   │   ├── invoices.tsx   # إدارة وسداد الفواتير وطباعة PDF
│   │   │   ├── chat.tsx       # محادثات الدعم الفني اللحظية والصوتية (Socket.IO)
│   │   │   └── ...
│   │   ├── services/api.ts    # طبقة الاتصال بالسيرفر والـ API
│   │   └── context/           # إدارة اللغات والثيم والمستخدم (SettingsContext)
│   ├── app.json               # إعدادات التطبيق ومتجر جوجل وأذونات الأندرويد
│   ├── eas.json               # إعدادات بناء الـ APK والـ AAB لمتجر جوجل
│   └── package.json
│
└── apex-backend/              # خادم الـ API والـ Realtime (Backend)
    ├── server.js              # السيرفر الرئيسي (Express + Socket.IO + SQLite)
    ├── setup_accounts.js      # سكربت تهيئة وتجهيز حسابات الأدمن والعميل
    ├── database.sqlite        # قاعدة البيانات المحلية (جاهزة ومجهزة بالبيانات)
    └── package.json
```

---

## ⚡ 2. خطوات التشغيل السريع (Local Development Setup)

### 🚀 خيار التشغيل الفوري بنقرة واحدة (One-Click Launch):
- **لمستخدمي ويندوز (Windows):** انقر مرتين على الملف: **`run_dev.bat`** في المجلد الرئيسي، وسيفتح السيرفر والتطبيق فوراً في نافذتين منفصلتين.
- **لمستخدمي ماك ولينكس (Mac / Linux):** شغّل في التيرمنال: **`./run_dev.sh`**.

---

### خيار التشغيل اليدوي خطوة بخطوة:

#### 1) تشغيل السيرفر (Backend)
1. افتح موجه الأوامر (Terminal) وانتقل إلى مجلد الباك إند:
   ```bash
   cd apex-backend
   ```
2. تثبيت الحزم (إذا لم تكن مثبتة):
   ```bash
   npm install
   ```
3. تشغيل السيرفر:
   ```bash
   npm start
   ```
   > يعمل السيرفر افتراضياً على: `http://localhost:3000`  
   > قاعدة البيانات من نوع SQLite مجهزة بوضع WAL عالي السرعة وبدون أي تعليق في القفل.  
   > لإعادة تهيئة الحسابات في أي وقت يمكن تشغيل: `npm run seed`

#### 2) تشغيل التطبيق (Frontend)
1. في نافذة Terminal أخرى، انتقل إلى مجلد التطبيق:
   ```bash
   cd apex-app
   ```
2. تثبيت الحزم:
   ```bash
   npm install
   ```
3. بدء تشغيل Expo:
   ```bash
   npx expo start
   ```
4. خيارات التشغيل:
   - **تشغيل على المتصفح (Web):** اضغط على حرف `w` في التيرمنال، أو افتح: `http://localhost:8081`.
   - **تشغيل على محاكي الأندرويد (Android Emulator):** اضغط على حرف `a`.
   - **تشغيل على هاتف أندرويد حقيقي:** ثبّت تطبيق **Expo Go** من متجر Google Play، ثم امسح رمز الـ QR الظاهر في التيرمنال (تأكد أن الهاتف والكمبيوتر متصلان بنفس شبكة الـ Wi-Fi).

---

## 👑 3. التحكم في صلاحيات الأدمن والمديرين (Admin Management)

يمكنك إدارة صلاحيات النظام بكل مرونة:

### 1) إنشاء أو ترقية أي حساب لأدمن عبر سطر الأوامر (CLI):
من داخل مجلد `apex-backend`:
```bash
npm run make-admin your-admin-email@example.com your-password
```
> يقوم السكربت بإنشاء الحساب وتعيين كلمة المرور وترقيته إلى `admin` فوراً.

### 2) التحكم في إيميلات المديرين عبر ملف `.env`:
يمكنه إنشاء ملف `.env` داخل `apex-backend` وكتابة إيميله أو أي إيميلات أخرى:
```env
ADMIN_EMAILS=dev@gmail.com,friend@company.com
PORT=3000
```
> أي إيميل مسجل في `ADMIN_EMAILS` يتم اعتماده كمدير بصلاحيات كاملة تلقائياً بمجرد تسجيل دخوله!

### 3) تعديل بيانات وهوية الوكالة والحسابات البنكية من الواجهة مباشرة:
لا داعي لتعديل أي كود في البنك أو أرقام كاش! داخل لوحة تحكم الأدمن (`/admin`):
- اضغط زر **`🏢 إعدادات الوكالة والبنك`**.
- يمكنك تغيير اسم الشركة، الهاتف، البريد، رقم السجل الضريبي، رقم فودافون كاش، الآيبان الدولي (IBAN)، انستاباي، وعنوان المقر بنقرة واحدة وتنعكس فوراً على فواتير كل العملاء وطباعة الـ PDF!

---

## 🎨 5. دليل إضافة وتطوير معرض الأعمال (Portfolio Guide)

الملف المخصص لمعرض الأعمال هو:
📂 **`apex-app/src/app/portfolio.tsx`**

### هيكل البيانات الحالي:
في بداية الملف تجد مصفوفة المشاريع:
```typescript
const projects = [
  { 
    id: '1', 
    name: isRTL ? 'تطبيق متجر إلكتروني' : 'E-Commerce App', 
    tech: 'React Native + Node.js', 
    image: 'https://images.unsplash.com/photo-1512941937669-90a1b58e7e9c?auto=format&fit=crop&w=600&q=80' 
  },
  // يمكنك إضافة مشاريع جديدة هنا بكل سهولة...
];
```

### أفكار واقتراحات لتطوير المعرض:
1. **إضافة حقول إضافية للمشروع**:
   - `category`: تصنيف المشروع (e.g. `'Mobile App'`, `'Web App'`, `'UI/UX'`).
   - `liveUrl`: رابط حقيقي للمشروع أو على Google Play / App Store.
   - `description`: نبذة تفصيلية عن المشكلة والحل ومميزات النظام.
2. **فلاتر التصنيفات (Category Filter Tabs)**:
   - إضافة شريط علوي (الكل، تطبيقات جوال، منصات ويب، تصميم واجهات).
3. **مودال تفاصيل المشروع (Project Details Modal)**:
   - عند النقر على كارت المشروع، يفتح نافذة منبثقة تحتوي على صور متعددة، رابط تجربة حية، والتقنيات المستخدمة بالتفصيل.

---

## 📲 6. دليل البناء والرفع على متجر Google Play Store

### أ) ضبط رابط السيرفر المباشر (Production API URL)
قبل بناء النسخة النهائية لمتجر جوجل، يجب أن يشير التطبيق إلى رابط السيرفر السحابي (وليس localhost):
1. داخل `apex-app`، أنشئ ملف `.env`:
   ```env
   EXPO_PUBLIC_API_URL=https://api.yourdomain.com
   ```
2. تم برمجة `apex-app/src/services/api.ts` ليلتقط `EXPO_PUBLIC_API_URL` تلقائياً.

### ب) التحقق من إعدادات الأندرويد في `app.json`
تم تجهيز `app.json` بالفعل بالقيم الأساسية:
- **Package Name:** `"com.apexsoftware.portal"` (يمكن تغييره لأي اسم تختاره).
- **Version:** `"1.0.0"`
- **Version Code:** `1` (يجب زيادته مع كل تحديث ترسله لجوجل).
- **الأذونات المطلوبة:** تم تضمين ميكروفون للرسائل الصوتية (`expo-av`) ومكتبة الصور لرفع الإيصالات وصور الملف الشخصي (`expo-image-picker`).

### ج) البناء باستخدام Expo EAS (الأسهل والأسرع)
1. تثبيت EAS CLI عالمياً (إن لم يكن مثبت):
   ```bash
   npm install -g eas-cli
   ```
2. تسجيل الدخول بحساب Expo الخاص بك:
   ```bash
   eas login
   ```
3. ربط المشروع بـ EAS:
   ```bash
   eas build:configure
   ```
4. **لبناء ملف APK تجريبي للتثبيت المباشر على الهواتف واختباره مع الفريق:**
   ```bash
   eas build --platform android --profile preview
   ```
5. **لبناء حزمة الإنتاج الرسمية AAB (Android App Bundle) لرفعها على Google Play Console:**
   ```bash
   eas build --platform android --profile production
   ```
   > ستقوم خوادم Expo السحابية بإنشاء وتوقيع ملف `.aab` جاهز للتحميل ورفعه فوراً على متجر جوجل بلاي.

---

---

## 📱 7. محركات تجربة الهاتف الذكي الفاخرة (Native Mobile Engine)

تم تزويد التطبيق بمحركات برمجية خاصة لتحويل تجربة الاستخدام إلى تطبيق هاتف ذكي نيتف (Native App) من الطراز الأول:

### 1️⃣ المصادقة البيومترية (FaceID & Fingerprint)
- **المكتبة المستخدمة:** `expo-local-authentication`
- **الملف المرجعي:** `apex-app/src/utils/biometrics.ts`
- **كيفية العمل:**
  - عند تسجيل الدخول بنجاح، يُتاح للمستخدم تفعيل الدخول ببصمة الإصبع أو الوجه بنقرة زر.
  - في المرات القادمة، يظهر زر ذهبي مباشر: `تسجيل الدخول السريع بالبصمة / FaceID`، دون الحاجة لإعادة كتابة البريد وكلمة المرور.
  - يمكن التحكم بتفعيلها أو إيقافها من شاشة `الإعدادات ⚙️`.

### 2️⃣ التنبيهات المنبثقة الحية (Push & Local Notifications)
- **المكتبة المستخدمة:** `expo-notifications`
- **الملف المرجعي:** `apex-app/src/utils/notifications.ts`
- **كيفية العمل:**
  - تسجيل التوكن الخاص بجهاز المستخدم ومزامنته مع السيرفر عبر `POST /api/user/push-token`.
  - إطلاق تنبيهات محلية فورية مصحوبة بصوت وإشعار في شريط التنبيهات (Notification Bar) عند:
    - سداد الفواتير بنجاح.
    - رفع إيصالات التحويل البنكي.
    - تحديث مراحل ومخرجات المشاريع.

### 3️⃣ محرك الاهتزازات اللمسية الفاخر (Haptic Feedback Engine)
- **المكتبة المستخدمة:** `expo-haptics`
- **الملف المرجعي:** `apex-app/src/utils/haptics.ts`
- **المؤثرات اللمسية المدمجة:**
  - `haptics.selection()` عند التبديل بين اللغات، التبويبات، ونسخ أرقام الحسابات.
  - `haptics.light()` عند النقر على الأزرار وقوائم التنقل وسحب الشاشة للتحديث (Pull-to-refresh).
  - `haptics.medium()` عند فتح روابط المخرجات والملفات الحية.
  - `haptics.success()` عند نجاح الدخول وسداد الفواتير.
  - `haptics.error()` عند أخطاء الإدخال أو التحقق.

### 4️⃣ وضع عدم الاتصال والتخزين المؤقت الذكي (Offline-First Mode)
- **الملف المرجعي:** `apex-app/src/utils/cache.ts`
- **كيفية العمل:**
  - يتم تخزين بيانات المستخدم، طلبات المشاريع، الفواتير، الإشعارات، وإعدادات الوكالة محلياً على جهاز المستخدم.
  - في حال انقطاع الإنترنت أو بطء الشبكة، يستمر التطبيق في العمل بسلاسة ويُعرض شريط تنبيهي أنيق:
    `⚡ وضع عدم الاتصال: يتم عرض البيانات المحفوظة محلياً` مع زر إعادة محاولة الاتصال فور عودة الشبكة.

> 🛡️ **أمان التوافق (Zero Web Crashes):** جميع هذه الموديولات تم برمجتها بمتحكمات ذكية بحيث تعمل على أندرويد وiOS بأعلى كفاءة نيتف، وعند التشغيل على متصفح الويب (Chrome/Safari) تعمل تلقائياً بأنظمة بديلة آمنة (Graceful Fallback) دون أي أخطاء أو توقف في الكود.

---

## 📋 8. قائمة متطلبات Google Play Console
عند رفع التطبيق على حساب المطورين في جوجل بلاي، جهز الآتي:
- **App Name**: `Apex Software`
- **Short Description**: الوكالة البرمجية الرائدة لتطوير التطبيقات والمواقع وحلول التحول الرقمي.
- **Privacy Policy URL**: رابط سياسة الخصوصية (متطلب إلزامي في جوجل بسبب أذونات الصور والمايك).
- **App Icon**: 512x512 PNG (موجود داخل `assets/images/icon.png`).
- **Feature Graphic**: 1024x500 PNG.
- **Screenshots**: لقطات شاشة للتطبيق (لوحة التحكم، الفواتير، الشات، حاسبة التكلفة، معرض الأعمال).

---

## 🔒 9. ملاحظات حول المدفوعات والخدمات الخارجية
- **بوابة الدفع Paymob:** تم الإبقاء على نظام محاكاة السداد بالبطاقة التجريبي حالياً، ولم يتم ربط Paymob الحقيقية بناءً على توجيهات الإدارة إلى حين استكمال فتح الحساب البنكي الرسمي والسجل التجاري للشركة.
- **التحويلات البنكية ومحافظ كاش:** تعمل بالكامل وبشكل ديناميكي من خلال زر `🏢 إعدادات الوكالة` داخل لوحة الأدمن.

بالتوفيق في الإطلاق! لأي استفسار تقني حول الأكواد، البنية البرمجية نظيفة ومعيارية بالكامل وسهلة التطوير.
